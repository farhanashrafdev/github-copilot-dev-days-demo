import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Keep the build output boring and fast. This app is a teaching instrument,
  // not a performance showcase — every option here must justify itself.
};

export default nextConfig;
